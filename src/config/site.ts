/** Production site URL (custom domain). Used for docs and optional client references. */
export const PRODUCTION_SITE_URL = "https://skillwrite.org";

export const PRODUCTION_SITE_ORIGIN = new URL(PRODUCTION_SITE_URL).origin;
